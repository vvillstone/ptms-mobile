<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical">

    <!-- Icône du type de section -->
    <ImageView
        android:id="@+id/iv_section_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_chat_bubble_outline"
        android:tint="@color/colorPrimary"
        android:contentDescription="@string/chat_section_icon" />

    <!-- Titre de la section -->
    <TextView
        android:id="@+id/tv_section_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:text="Conversations privées"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="@color/colorPrimary" />

    <!-- Nombre d'éléments -->
    <TextView
        android:id="@+id/tv_section_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="12dp"
        android:text="3"
        android:textSize="14sp"
        android:textColor="@android:color/darker_gray" />

    <!-- Flèche de pli/dépli -->
    <ImageView
        android:id="@+id/iv_section_expand"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/ic_arrow_right"
        android:tint="@android:color/darker_gray"
        android:rotation="90"
        android:contentDescription="@string/chat_expand_section" />

</LinearLayout>
